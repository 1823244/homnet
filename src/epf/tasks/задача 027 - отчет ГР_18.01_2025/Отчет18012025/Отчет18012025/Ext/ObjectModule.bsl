
#Область ПодключениеОбработкиКБСП

Функция СведенияОВнешнейОбработке() Экспорт

	ПараметрыРегистрации = ДополнительныеОтчетыИОбработки.СведенияОВнешнейОбработке();

	ПараметрыРегистрации.Вставить("Вид",ДополнительныеОтчетыИОбработкиКлиентСервер.ВидОбработкиДополнительнаяОбработка());
	ПараметрыРегистрации.Вставить("Версия","1.14");
	//ПараметрыРегистрации.Вставить("Назначение", Новый Массив);
	ПараметрыРегистрации.Вставить("Наименование","Отчет18012025");
	ПараметрыРегистрации.Вставить("БезопасныйРежим", Ложь);
	ПараметрыРегистрации.Вставить("Информация","Отчет 18.01_2025");
	ПараметрыРегистрации.Вставить("ВерсияБСП", "3.1.5.180");
	//ПараметрыРегистрации.Вставить("ОпределитьНастройкиФормы", Ложь);

	ТипКоманды = ДополнительныеОтчетыИОбработкиКлиентСервер.ТипКомандыОткрытиеФормы();
	ДобавитьКоманду(ПараметрыРегистрации.Команды,
		"Открыть форму : Отчет 18.01_2025",
		"Форма_Отчет18012025",
		ТипКоманды,
		Ложь) ;
	//	
	//ТипКоманды = ДополнительныеОтчетыИОбработкиКлиентСервер.ТипКомандыВызовСерверногоМетода();
	//ДобавитьКоманду(ПараметрыРегистрации.Команды, 
	//	"Выполнить отложенное проведение",
	//	"ВыполнитьОтложенноеПроведение",
	//	ТипКоманды, 
	//	Ложь) ;

	Возврат ПараметрыРегистрации;
	
КонецФункции

Процедура ДобавитьКоманду(ТаблицаКоманд, Представление, Идентификатор, Использование, ПоказыватьОповещение = Ложь, Модификатор = "")

	//ТаблицаКоманд.Колонки.Добавить("Представление", РеквизитыТабличнойЧасти.Представление.Тип);
	//ТаблицаКоманд.Колонки.Добавить("Идентификатор", РеквизитыТабличнойЧасти.Идентификатор.Тип);
	//ТаблицаКоманд.Колонки.Добавить("Использование", Новый ОписаниеТипов("Строка"));
	//ТаблицаКоманд.Колонки.Добавить("ПоказыватьОповещение", РеквизитыТабличнойЧасти.ПоказыватьОповещение.Тип);
	//ТаблицаКоманд.Колонки.Добавить("Модификатор", РеквизитыТабличнойЧасти.Модификатор.Тип);
	//ТаблицаКоманд.Колонки.Добавить("Скрыть",      РеквизитыТабличнойЧасти.Скрыть.Тип);
	//ТаблицаКоманд.Колонки.Добавить("ЗаменяемыеКоманды", РеквизитыТабличнойЧасти.ЗаменяемыеКоманды.Тип);
	
//           ** Использование - Строка - тип команды:
//               "ВызовКлиентскогоМетода",
//               "ВызовСерверногоМетода",
//               "ЗаполнениеФормы",
//               "ОткрытиеФормы" или
//               "СценарийВБезопасномРежиме".
//               Для получения типов команд рекомендуется использовать функции
//               ДополнительныеОтчетыИОбработкиКлиентСервер.ТипКоманды<ИмяТипа>.
//               В комментариях к этим функциям также даны шаблоны процедур-обработчиков команд.

	НоваяКоманда = ТаблицаКоманд.Добавить() ;
	НоваяКоманда.Представление = Представление ;
	НоваяКоманда.Идентификатор = Идентификатор ;
	НоваяКоманда.Использование = Использование ;
	НоваяКоманда.ПоказыватьОповещение = ПоказыватьОповещение ;
	НоваяКоманда.Модификатор = Модификатор ;
КонецПроцедуры

// Интерфейс для запуска логики обработки.
Процедура ВыполнитьКоманду(ИмяКоманды, ПараметрыВыполнения) Экспорт
	
	Если ИмяКоманды = "ВыполнитьОтложенноеПроведение" Тогда
		//ВыполнитьОтложенноеПроведение();
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

// Параметры:
//	Организация - спр ссылка
//	СчетСсылка - план счетов Хозрасчетный
//	МассивСсылок - массив - ссылки на субконто, спр НематериальныеАктивы
//	_НачалоПериода - дата
//	_КонецПериода - дата
//	ЭтоДебетовыйОборот - булево - управляет видом оборота: по Дт или по Кт
//
Функция ОборотыПоСчету(Организация, СчетСсылка, МассивСсылок, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот=Истина) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ХозрасчетныйОстаткиИОбороты.Субконто1.хн_нфо_ВидНМА КАК Субконто1,
		|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаОборот) КАК СуммаОборот,
		|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаОборотДт) КАК СуммаОборотДт,
		|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаОборотКт) КАК СуммаОборотКт,
		|	СУММА(ХозрасчетныйОстаткиИОбороты.ВалютнаяСуммаОборот) КАК ВалютнаяСуммаОборот,
		|	СУММА(ХозрасчетныйОстаткиИОбороты.ВалютнаяСуммаОборотДт) КАК ВалютнаяСуммаОборотДт,
		|	СУММА(ХозрасчетныйОстаткиИОбороты.ВалютнаяСуммаОборотКт) КАК ВалютнаяСуммаОборотКт
		|ИЗ
		|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты(
		|			&НачалоПериода,
		|			&КонецПериода,
		|			Период,
		|			,
		|			Счет В ИЕРАРХИИ (&Счет),
		|			,
		|			Организация = &Организация
		|				И Субконто1 В (&Субконто1)) КАК ХозрасчетныйОстаткиИОбороты
		|СГРУППИРОВАТЬ ПО
		|	ХозрасчетныйОстаткиИОбороты.Субконто1.хн_нфо_ВидНМА";

	Запрос.УстановитьПараметр("НачалоПериода", НачалоДня(_НачалоПериода));
	Запрос.УстановитьПараметр("КонецПериода", КонецДня(_КонецПериода));
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("Субконто1", МассивСсылок);
	Запрос.УстановитьПараметр("Счет", СчетСсылка);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		Если ЭтоДебетовыйОборот Тогда
		
			возврат ВыборкаДетальныеЗаписи.СуммаОборотДт;
		
		Иначе
			
			возврат ВыборкаДетальныеЗаписи.СуммаОборотКт;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат 0;
	
КонецФункции

// Параметры:
//	Организация - спр ссылка
//	СчетСсылка - план счетов Хозрасчетный
//	МассивСсылок - массив - ссылки на субконто, спр НематериальныеАктивы
//	ДатаОстатка - дата
//	ЭтоОстатокНаНачало - булево - управляет видом границы для регистра бухгалтерии
//
Функция ОстатокПоСчету(Организация, СчетСсылка, МассивСсылок, ДатаОстатка, ЭтоОстатокНаНачало=Истина) Экспорт

	Если ЭтоОстатокНаНачало=Истина Тогда

		Парам = Новый Массив(2);
		Парам[0] = НачалоДня(ДатаОстатка);
		Парам[1] = ВидГраницы.Исключая;
		Граница = Новый(Тип("Граница"),Парам);

	ИначеЕсли ЭтоОстатокНаНачало=Ложь Тогда

		Парам = Новый Массив(2);
		Парам[0] = КонецДня(ДатаОстатка);
		Парам[1] = ВидГраницы.Включая;
		Граница = Новый(Тип("Граница"),Парам);

	КонецЕсли;

	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ХозрасчетныйОстатки.Субконто1.хн_нфо_ВидНМА КАК Субконто1,
		|	СУММА(ХозрасчетныйОстатки.СуммаОстаток) КАК СуммаОстаток,
		|	СУММА(ХозрасчетныйОстатки.СуммаОстатокДт) КАК СуммаОстатокДт,
		|	СУММА(ХозрасчетныйОстатки.СуммаОстатокКт) КАК СуммаОстатокКт,
		|	СУММА(ХозрасчетныйОстатки.ВалютнаяСуммаОстаток) КАК ВалютнаяСуммаОстаток,
		|	СУММА(ХозрасчетныйОстатки.ВалютнаяСуммаОстатокДт) КАК ВалютнаяСуммаОстатокДт,
		|	СУММА(ХозрасчетныйОстатки.ВалютнаяСуммаОстатокКт) КАК ВалютнаяСуммаОстатокКт
		|ИЗ
		|	РегистрБухгалтерии.Хозрасчетный.Остатки(
		|			&ДатаОстатка,
		|			Счет В ИЕРАРХИИ (&Счет),
		|			,
		|			Организация = &Организация
		|				И Субконто1 В (&Субконто1)) КАК ХозрасчетныйОстатки
		|
		|СГРУППИРОВАТЬ ПО
		|	ХозрасчетныйОстатки.Субконто1.хн_нфо_ВидНМА";
	
	Запрос.УстановитьПараметр("ДатаОстатка", Граница);
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("Субконто1", МассивСсылок);
	Запрос.УстановитьПараметр("Счет", СчетСсылка);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		Возврат ВыборкаДетальныеЗаписи.СуммаОстаток;
	КонецЦикла;
	
	Возврат 0;
              
КонецФункции

// Назначение: Основная процедура запуска создания всех показателей отчета ГР 18.01.2025
// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
// Операции с БД: Создание группы показателей, массовое создание элементов справочника
// Валидация: Проверка существования группы и показателей
Процедура СоздатьОбновитьПоказатели(Знач ВыводитьСообщения = Истина) Экспорт
 
	#region initialization
	
	Если ВыводитьСообщения Тогда
		Сообщить("<operation_start>Начало создания показателей отчета ГР 18.01.2025</operation_start>");
	КонецЕсли;
	
	#endregion
	
	#region group_preparation
	
	СоздатьГруппуПоказателей(ВыводитьСообщения);
	ПометитьВсеПоказателиНаУдаление(ВыводитьСообщения);
	
	#endregion

	СоздатьПоказателиСтроки2(ВыводитьСообщения);
	СоздатьПоказателиСтроки3(ВыводитьСообщения);

	// сумма строк
	СоздатьПоказателиСтроки1(ВыводитьСообщения);

	СоздатьПоказателиСтроки5(ВыводитьСообщения);

	СоздатьПоказателиСтроки9(ВыводитьСообщения);
	СоздатьПоказателиСтроки10(ВыводитьСообщения);
	// сумма строк
	СоздатьПоказателиСтроки8(ВыводитьСообщения);

	СоздатьПоказателиСтроки12(ВыводитьСообщения);
	
	//// сумма строк - но строки 14 и 15 в моем ТЗ - не имеют формул
	//СоздатьПоказателиСтроки13(ВыводитьСообщения);

	//// сумма строк - но строки 17 и 18 в моем ТЗ - не имеют формул
	//СоздатьПоказателиСтроки16(ВыводитьСообщения);
	//
	//// сумма строк - но строки 20 и 21 в моем ТЗ - не имеют формул
	//СоздатьПоказателиСтроки19(ВыводитьСообщения);

	//// сумма строк - 
	СоздатьПоказателиСтроки23(ВыводитьСообщения);
	
	СоздатьПоказателиСтроки24(ВыводитьСообщения);
	СоздатьПоказателиСтроки25(ВыводитьСообщения);
	
	СоздатьПоказателиСтроки28(ВыводитьСообщения);
	СоздатьПоказателиСтроки29(ВыводитьСообщения);

	//// Бал стоим. сумма строк - 28+29+30  (30 - нет формул)
	СоздатьПоказателиСтроки27(ВыводитьСообщения);

	СоздатьПоказателиСтроки31(ВыводитьСообщения);

	СоздатьПоказателиСтроки35(ВыводитьСообщения);
	СоздатьПоказателиСтроки36(ВыводитьСообщения);

	//// сумма строк - 35+36+37  (37 - нет формул)
	СоздатьПоказателиСтроки34(ВыводитьСообщения);
	
	СоздатьПоказателиСтроки38(ВыводитьСообщения);

	//// сумма строк - 40+41   - нет формул
	//СоздатьПоказателиСтроки39(ВыводитьСообщения);
	//
	//// сумма строк - 43+44   - нет формул
	//СоздатьПоказателиСтроки42(ВыводитьСообщения);
	//
	//// сумма строк - 46+47   - нет формул
	//СоздатьПоказателиСтроки45(ВыводитьСообщения);

	//// Бал стоим
	СоздатьПоказателиСтроки49(ВыводитьСообщения);
	
	СоздатьПоказателиСтроки50(ВыводитьСообщения);
	
	СоздатьПоказателиСтроки51(ВыводитьСообщения);

	#region operation_completion
	
	Если ВыводитьСообщения Тогда
		Сообщить("<operation_end>Завершено создание показателей отчета ГР_18.1_2025</operation_end>");
	КонецЕсли;
	
	#endregion

КонецПроцедуры

// Назначение: Создание группы показателей ГР_18.01_2025
// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
// Операции с БД: Создание элемента справочника-группы
// Валидация: Проверка существования группы перед созданием
Процедура СоздатьГруппуПоказателей(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18.01_2025";
	НаименованиеГруппы = "ГР_18.01_2025";
	
	#endregion
	
	#region validation
	
	ГруппаСсылка = Справочники.хн_нфо_ПоказателиОтчета.НайтиПоНаименованию(НаименованиеГруппы,истина);
	
	#endregion
	
	#region group_creation
	
	Если НЕ ЗначениеЗаполнено(ГруппаСсылка) Тогда
		
		ГруппаОбъект = Справочники.хн_нфо_ПоказателиОтчета.СоздатьГруппу();
		ГруппаОбъект.Идентификатор = Идентификатор;
		ГруппаОбъект.Наименование = НаименованиеГруппы;
		ГруппаОбъект.Записать();
		
		Если ВыводитьСообщения Тогда
			Сообщить("<data_summary>Создана группа показателей: " + НаименованиеГруппы+ "</data_summary>");
		КонецЕсли;
		
	Иначе
		
		Если ВыводитьСообщения Тогда
			Сообщить("<validation_result>Группа показателей уже существует: " + НаименованиеГруппы + "</validation_result>");
		КонецЕсли;
		
	КонецЕсли;
	
	#endregion

КонецПроцедуры

// Назначение: Пометка всех показателей группы ГР_18.01_2025 на удаление
// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
// Операции с БД: Обновление пометки удаления элементов справочника
// Производительность: Пакетная обработка через запрос
Процедура ПометитьВсеПоказателиНаУдаление(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	ИмяГруппы = "ГР_18.01_2025";
	
	#endregion
	
	#region database_query
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	хн_нфо_ПоказателиОтчета.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.хн_нфо_ПоказателиОтчета КАК хн_нфо_ПоказателиОтчета
	|ГДЕ
	|	хн_нфо_ПоказателиОтчета.Родитель = &Родитель
	|	И НЕ хн_нфо_ПоказателиОтчета.ПометкаУдаления";
	
	ГруппаСсылка = Справочники.хн_нфо_ПоказателиОтчета.НайтиПоНаименованию(ИмяГруппы, Истина);
	Запрос.УстановитьПараметр("Родитель", ГруппаСсылка);
	
	Результат = Запрос.Выполнить();
	
	#endregion
	
	#region loop_processing
	
	КоличествоПомеченных = 0;
	Выборка = Результат.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		ПоказательОбъект = Выборка.Ссылка.ПолучитьОбъект();
		//ПоказательОбъект.УстановитьПометкуУдаления(Истина);
		ПоказательОбъект.ПометкаУдаления = Истина;
		ПоказательОбъект.Записать();
		КоличествоПомеченных = КоличествоПомеченных + 1;
		
	КонецЦикла;
	
	#endregion
	
	#region result_formation
	
	Если ВыводитьСообщения Тогда
		Сообщить("<data_summary>Помечено на удаление показателей: " + КоличествоПомеченных + "</data_summary>");
	КонецЕсли;
	
	#endregion

КонецПроцедуры

Процедура СоздатьИлиОбновитьПоказатель(
	Идентификатор,
	НаименованиеПоказателя,
	ВидПоказателя,
	ТипПоказателяОтчета,
	ОписаниеПоказателя,
	СписокИспользуемыхПоказателей = "",
	ЗначениеОтбора = Неопределено,
	Знач ВыводитьСообщения = Ложь,
	ТекстКода = "",
	Формула = "",
	Множитель = 1)
	
	#region initialization
	
	ИмяГруппы = "ГР_18.01_2025";
	
	#endregion
	
	#region validation
	
	ГруппаСсылка = Справочники.хн_нфо_ПоказателиОтчета.НайтиПоНаименованию(ИмяГруппы, Истина);
	Если НЕ ЗначениеЗаполнено(ГруппаСсылка) Тогда
		
		Если ВыводитьСообщения Тогда
			Сообщить("<error_details>Ошибка: Группа " + ИмяГруппы + " не найдена</error_details>");
		КонецЕсли;
		Возврат;
		
	КонецЕсли;
	
	ПоказательСсылка = Справочники.хн_нфо_ПоказателиОтчета.НайтиПоНаименованию(НаименованиеПоказателя, Истина);
	
	#endregion
	
	#region indicator_creation
	
	Если НЕ ЗначениеЗаполнено(ПоказательСсылка) Тогда
		ПоказательОбъект = Справочники.хн_нфо_ПоказателиОтчета.СоздатьЭлемент();
	Иначе
		ПоказательОбъект = ПоказательСсылка.ПолучитьОбъект();
	КонецЕсли;
	
	#endregion
	
	#region data_processing
	
	// Заполнение основных атрибутов
	ПоказательОбъект.Наименование = НаименованиеПоказателя;
	ПоказательОбъект.Родитель = ГруппаСсылка;

	ПоказательОбъект.Идентификатор = Идентификатор;
	ПоказательОбъект.ВидПоказателя = ВидПоказателя;
	ПоказательОбъект.Формула = Формула;
	ПоказательОбъект.ОписаниеПоказателя = ОписаниеПоказателя;
	ПоказательОбъект.НеВыводитьВОтчет = Ложь;
	ПоказательОбъект.ТекстКода = ТекстКода;
	ПоказательОбъект.Множитель = Множитель;
	ПоказательОбъект.ВалютаПоказателя = Undefined;
	
	ПоказательОбъект.ФорматДаты = "";
	ПоказательОбъект.ТипРегистратора = "";
	ПоказательОбъект.ТипПоказателяОтчета = ТипПоказателяОтчета;
	
	// Заполнение атрибутов в зависимости от вида показателя
	Если ВидПоказателя = Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле Тогда
		
		ПоказательОбъект.Регистр = "";
		ПоказательОбъект.Ресурс = "";
		ПоказательОбъект.ТипПоказателя = 1; // Строка для формульных
		
	Иначе
		
		ПоказательОбъект.ТипПоказателя = 0; // Число
		ПоказательОбъект.Регистр = "Хозрасчетный";
		
		Если ТипПоказателяОтчета = Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало Тогда
			ПоказательОбъект.Ресурс = "СуммаНачальныйОстаток";
		ИначеЕсли ТипПоказателяОтчета = Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаКонец Тогда
			ПоказательОбъект.Ресурс = "СуммаКонечныйОстаток";
		ИначеЕсли ТипПоказателяОтчета = Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод Тогда
			ПоказательОбъект.Ресурс = "СуммаОборот";
		КонецЕсли;
		
	КонецЕсли;
	
	#endregion
	
	#region tabular_section_processing
	
	// Заполнение табличной части для формульных показателей
	Если ВидПоказателя = Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле
		И НЕ ПустаяСтрока(СписокИспользуемыхПоказателей) Тогда
		
		ПоказательОбъект.ИспользуемыеПоказатели.Очистить();
		
		МассивИмен = СтрРазделить(СписокИспользуемыхПоказателей, ",", Ложь);
		Для каждого ИмяИспользуемого Из МассивИмен Цикл
			
			ИмяИспользуемого = СокрЛП(ИмяИспользуемого);
			ИспользуемыйПоказатель = Справочники.хн_нфо_ПоказателиОтчета.НайтиПоНаименованию(ИмяИспользуемого, Истина);
			
			Если ЗначениеЗаполнено(ИспользуемыйПоказатель) Тогда
				
				НоваяСтрока = ПоказательОбъект.ИспользуемыеПоказатели.Добавить();
				НоваяСтрока.Показатель = ИспользуемыйПоказатель;
				НоваяСтрока.Множитель = Множитель;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	#endregion
	
	#region database_write
	
	//ПоказательОбъект.УстановитьПометкуУдаления(Ложь);
	ПоказательОбъект.ПометкаУдаления = Ложь;
	ПоказательОбъект.Записать();
	
	#endregion
	
	#region result_formation
	
	Если ВыводитьСообщения Тогда
		Сообщить("<data_summary>Создан/обновлен показатель: " + НаименованиеПоказателя + "</data_summary>");
	КонецЕсли;
	
	#endregion

КонецПроцедуры

//МассивСубконто=ПолучитьМассивСубконто(Перечисления.ВидыНематериальныхАктивов.ПрограммыЭВМ);
//МассивСубконто=ПолучитьМассивСубконто(Перечисления.ВидыНематериальныхАктивов.ЛицензииРазрешения);
//МассивСубконто=ПолучитьМассивСубконто(Перечисления.ВидыНематериальныхАктивов.ДругиеНМА);
Функция ПолучитьМассивСубконто(видНМА) Экспорт

	запрос=новый запрос("Выбрать ссылка из справочник.НематериальныеАктивы где хн_нфо_ВидНМА = &ВидНематериальногоАктива");
	запрос.установитьпараметр("ВидНематериальногоАктива",видНМА);
	ТЗ=запрос.выполнить().выгрузить();
	массивНМА=ТЗ.ВыгрузитьКолонку("Ссылка");
	Возврат массивНМА;

КонецФункции

Функция НМА_ПО() Экспорт
	возврат  ПолучитьМассивСубконто(Перечисления.хн_нфо_ВидыНМА.ПрограммноеОбеспечение);
КонецФункции

Функция НМА_Лиц() Экспорт
	возврат  ПолучитьМассивСубконто(Перечисления.хн_нфо_ВидыНМА.ЛицензииИФраншизы);
КонецФункции

Функция НМА_Прочие() Экспорт
	возврат  ПолучитьМассивСубконто(Перечисления.хн_нфо_ВидыНМА.Прочее);
КонецФункции

#Область ПредыдущийПериодОтчета

//////////  СТРОКА 1

Процедура СоздатьПоказателиСтроки1(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр1кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол10(ВыводитьСообщения);
	
КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр1кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 3 + строка 4";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр2кол3,ГР_18_01_2025_стр3кол3";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр1кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 3 + строка 4";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр2кол4,ГР_18_01_2025_стр3кол4";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр1кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 3 + строка 4";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр2кол5,ГР_18_01_2025_стр3кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр1кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 1 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр1кол3,ГР_18_01_2025_стр1кол4,ГР_18_01_2025_стр1кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 2

Процедура СоздатьПоказателиСтроки2(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр2кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол10(ВыводитьСообщения);

КонецПроцедуры

// Назначение: Создание показателя строка 2 колонка 3 (остаток исключительных программ на начало)
// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр2кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр2кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60901.ПС Субконто1. Вид НМА (НФО) «Исключительные (исследовательские) программы» на начало предыдущего периода";
	
	#endregion
	
	#region indicator_creation
	//Остаток по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» на начало предыдущего периода
	
	//todo уточнить, какой справочник в реальной конфигурации Хомнет

	ТекстКода = "
	|СсылкаОбработчика = Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика = ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|Префикс = ""ГР 18.01_2025, строка 2 колонка 3::"";
	|ЭтоОстатокНаНачало = Истина;
	|СчетСсылка = ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|ДатаОстатка = НачалоГода(НачалоГода(мНачалоПериода)-1);
	|СубконтоСсылка = ОбъектОбработчика.НМА_ПО(); //массив
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|//ОбъектОбработчика.НМА_Лиц()    ОбъектОбработчика.НМА_Прочие()
	|Сообщить(""--------------------------------------------"");
	|Сообщить(Префикс);
    |Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";
	
	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр2кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр2кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» на начало предыдущего периода";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|ДатаОстатка=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 2 колонка 4::"";
	|Сообщить(Префикс);
    |Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр2кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр2кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Прочие» на начало предыдущего периода";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|ДатаОстатка=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 2 колонка 5::"";
	|Сообщить(Префикс);
    |Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр2кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр2кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 2 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр2кол3,ГР_18_01_2025_стр2кол4,ГР_18_01_2025_стр2кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 3

Процедура СоздатьПоказателиСтроки3(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр3кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол10(ВыводитьСообщения);

КонецПроцедуры

// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр3кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр3кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» на начало предыдущего периода";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|ДатаОстатка=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 3 колонка 3::"";
	|Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр3кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» на начало предыдущего периода";
	
	#endregion
	
	#region indicator_creation
	
	//todo уточнить, какой справочник в реальной конфигурации Хомнет

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|ДатаОстатка=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 3 колонка 4::"";
	|Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр3кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Прочие» на начало предыдущего периода";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|ДатаОстатка=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 3 колонка 5::"";
	|Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр3кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 3 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр3кол3,ГР_18_01_2025_стр3кол4,ГР_18_01_2025_стр3кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 5

Процедура СоздатьПоказателиСтроки5(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр5кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол10(ВыводитьСообщения);

КонецПроцедуры

// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр5кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр5кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по дебету по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» в отчетном периоде предыдущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|_НачалоПериода=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|_КонецПериода = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(_КонецПериода)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(_КонецПериода) Тогда
	|		_КонецПериода = КонецМесяца(_КонецПериода);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 5 колонка 3::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр5кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр5кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по дебету по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» в  отчетном периоде предыдущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|_НачалоПериода=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|_КонецПериода = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(_КонецПериода)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(_КонецПериода) Тогда
	|		_КонецПериода = КонецМесяца(_КонецПериода);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 5 колонка 4::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр5кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр5кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по дебету по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Прочие» в  отчетном периоде предыдущего года";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|_НачалоПериода=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|_КонецПериода = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(_КонецПериода)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(_КонецПериода) Тогда
	|		_КонецПериода = КонецМесяца(_КонецПериода);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 5 колонка 5::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр5кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр5кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 3 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр5кол3,ГР_18_01_2025_стр5кол4,ГР_18_01_2025_стр5кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 8

Процедура СоздатьПоказателиСтроки8(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр8кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр8кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр8кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр8кол10(ВыводитьСообщения);
	
КонецПроцедуры

Процедура СоздатьПоказатель_стр8кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр8кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 9 + строка 10";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр9кол3,ГР_18_01_2025_стр10кол3";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр8кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр8кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 9 + строка 10";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр9кол4,ГР_18_01_2025_стр10кол4";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр8кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр8кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 9 + строка 10";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр9кол5,ГР_18_01_2025_стр10кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр8кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр8кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 1 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр8кол3,ГР_18_01_2025_стр8кол4,ГР_18_01_2025_стр8кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 9

Процедура СоздатьПоказателиСтроки9(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр9кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр9кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр9кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр9кол10(ВыводитьСообщения);

КонецПроцедуры

// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр9кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр9кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по кредиту по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» в отчетном периоде предыдущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|_НачалоПериода=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|_КонецПериода = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(_КонецПериода)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(_КонецПериода) Тогда
	|		_КонецПериода = КонецМесяца(_КонецПериода);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = -1*ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 9 колонка 3::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр9кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр9кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по кредиту по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» в  отчетном периоде предыдущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|_НачалоПериода=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|_КонецПериода = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(_КонецПериода)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(_КонецПериода) Тогда
	|		_КонецПериода = КонецМесяца(_КонецПериода);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = -1*ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 9 колонка 4::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр9кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр9кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по кредиту по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Прочие» в  отчетном периоде предыдущего года";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|_НачалоПериода=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|_КонецПериода = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(_КонецПериода)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(_КонецПериода) Тогда
	|		_КонецПериода = КонецМесяца(_КонецПериода);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = -1*ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 9 колонка 5::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр9кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр9кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 3 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр9кол3,ГР_18_01_2025_стр9кол4,ГР_18_01_2025_стр9кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 10

Процедура СоздатьПоказателиСтроки10(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр10кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр10кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр10кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр10кол10(ВыводитьСообщения);

КонецПроцедуры

// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр10кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр10кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот дебету  по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» в отчетном периоде предыдущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|_НачалоПериода=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|_КонецПериода = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(_КонецПериода)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(_КонецПериода) Тогда
	|		_КонецПериода = КонецМесяца(_КонецПериода);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 10 колонка 3::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр10кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр10кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот дебету  по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» в отчетном периоде предыдущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|_НачалоПериода=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|_КонецПериода = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(_КонецПериода)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(_КонецПериода) Тогда
	|		_КонецПериода = КонецМесяца(_КонецПериода);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 10 колонка 4::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр10кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр10кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот дебету  по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Прочие» в отчетном периоде предыдущего года";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|_НачалоПериода=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|_КонецПериода = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(_КонецПериода)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(_КонецПериода) Тогда
	|		_КонецПериода = КонецМесяца(_КонецПериода);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 10 колонка 5::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр10кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр10кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 3 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр10кол3,ГР_18_01_2025_стр10кол4,ГР_18_01_2025_стр10кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 12

Процедура СоздатьПоказателиСтроки12(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр12кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр12кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр12кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр12кол10(ВыводитьСообщения);

КонецПроцедуры

// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр12кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр12кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот кредиту  по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» в отчетном периоде предыдущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|_НачалоПериода=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|_КонецПериода = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(_КонецПериода)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(_КонецПериода) Тогда
	|		_КонецПериода = КонецМесяца(_КонецПериода);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = -1*ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 12 колонка 3::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр12кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр12кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот кредиту  по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» в отчетном периоде предыдущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|_НачалоПериода=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|_КонецПериода = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(_КонецПериода)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(_КонецПериода) Тогда
	|		_КонецПериода = КонецМесяца(_КонецПериода);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = -1*ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 12 колонка 4::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр12кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр12кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот кредиту  по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Прочие» в отчетном периоде предыдущего года";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|_НачалоПериода=НачалоГода(НачалоГода(мНачалоПериода)-1);
	|_КонецПериода = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(_КонецПериода)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(_КонецПериода) Тогда
	|		_КонецПериода = КонецМесяца(_КонецПериода);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = -1*ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка 12 колонка 5::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр12кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр12кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 3 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр12кол3,ГР_18_01_2025_стр12кол4,ГР_18_01_2025_стр12кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 23

Процедура СоздатьПоказателиСтроки23(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр23кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр23кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр23кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр23кол10(ВыводитьСообщения);
	
КонецПроцедуры

Процедура СоздатьПоказатель_стр23кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр23кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "=строка 1 + строка 5 + строка 6 + строка 7 + строка 8 + строка 12 + строка 13 + строка 16 + строка 19 + строка 22";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр1кол3,ГР_18_01_2025_стр5кол3,ГР_18_01_2025_стр8кол3,ГР_18_01_2025_стр12кол3";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр23кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр23кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "=строка 1 + строка 5 + строка 6 + строка 7 + строка 8 + строка 12 + строка 13 + строка 16 + строка 19 + строка 22";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр1кол4,ГР_18_01_2025_стр5кол4,ГР_18_01_2025_стр8кол4,ГР_18_01_2025_стр12кол4";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр23кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр23кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "=строка 1 + строка 5 + строка 6 + строка 7 + строка 8 + строка 12 + строка 13 + строка 16 + строка 19 + строка 22";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр1кол5,ГР_18_01_2025_стр5кол5,ГР_18_01_2025_стр8кол5,ГР_18_01_2025_стр12кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр23кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр23кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 23 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр23кол3,ГР_18_01_2025_стр23кол4,ГР_18_01_2025_стр23кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 24

Процедура СоздатьПоказателиСтроки24(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр24кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр24кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр24кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр24кол10(ВыводитьСообщения);

КонецПроцедуры

// Назначение: Создание показателя строка 2 колонка 3 (остаток исключительных программ на начало)
// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр24кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр24кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60901.ПС Субконто1. Вид НМА (НФО) «Исключительные (исследовательские) программы» на конец предыдущего периода";
	
	#endregion
	
	#region indicator_creation
	//Остаток по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» на начало предыдущего периода
	
	//todo уточнить, какой справочник в реальной конфигурации Хомнет

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|ДатаОстатка = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(ДатаОстатка)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(ДатаОстатка) Тогда
	|		ДатаОстатка = КонецМесяца(ДатаОстатка);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр24кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр24кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» на конец предыдущего периода";
	
	#endregion
	
	#region indicator_creation
	
	//todo уточнить, какой справочник в реальной конфигурации Хомнет

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|ДатаОстатка = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(ДатаОстатка)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(ДатаОстатка) Тогда
	|		ДатаОстатка = КонецМесяца(ДатаОстатка);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр24кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр24кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Прочие» на конец предыдущего периода";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|ДатаОстатка = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(ДатаОстатка)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(ДатаОстатка) Тогда
	|		ДатаОстатка = КонецМесяца(ДатаОстатка);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр24кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр24кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 24 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр24кол3,ГР_18_01_2025_стр24кол4,ГР_18_01_2025_стр24кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 25

Процедура СоздатьПоказателиСтроки25(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр25кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр25кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр25кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр25кол10(ВыводитьСообщения);

КонецПроцедуры

// Назначение: Создание показателя строка 2 колонка 3 (остаток исключительных программ на начало)
// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр25кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр25кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60903.ПС Субконто1. Вид НМА (НФО) «Исключительные (исследовательские) программы» на конец предыдущего периода";
	
	#endregion
	
	#region indicator_creation
	//Остаток по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» на начало предыдущего периода
	
	//todo уточнить, какой справочник в реальной конфигурации Хомнет

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|ДатаОстатка = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(ДатаОстатка)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(ДатаОстатка) Тогда
	|		ДатаОстатка = КонецМесяца(ДатаОстатка);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр25кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр25кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» на конец предыдущего периода";
	
	#endregion
	
	#region indicator_creation
	
	//todo уточнить, какой справочник в реальной конфигурации Хомнет

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|ДатаОстатка = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(ДатаОстатка)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(ДатаОстатка) Тогда
	|		ДатаОстатка = КонецМесяца(ДатаОстатка);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр25кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр25кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Прочие» на конец предыдущего периода";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|ДатаОстатка = ДобавитьМесяц(мКонецПериода, -12);
	|Если КонецДня(мКонецПериода) = КонецМесяца(мКонецПериода)
	|	И Месяц(ДатаОстатка)=2 Тогда
	|	Если ОбщегоНазначенияБЗККлиентСервер.ЭтоВисокосныйГод(ДатаОстатка) Тогда
	|		ДатаОстатка = КонецМесяца(ДатаОстатка);
	|	КонецЕсли;
	|КонецЕсли;
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр25кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр25кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 24 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр25кол3,ГР_18_01_2025_стр25кол4,ГР_18_01_2025_стр25кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

#КонецОбласти

#Область ТекущийПериодОтчета
	
//////////  СТРОКА 27

Процедура СоздатьПоказателиСтроки27(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр27кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр27кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр27кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр27кол10(ВыводитьСообщения);
	
КонецПроцедуры

Процедура СоздатьПоказатель_стр27кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр27кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 28 + строка 29 + строка 30";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр28кол3,ГР_18_01_2025_стр29кол3";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр27кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр27кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр28кол4,ГР_18_01_2025_стр29кол4";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр27кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр27кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр28кол5,ГР_18_01_2025_стр29кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр27кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр27кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр27кол3,ГР_18_01_2025_стр27кол4,ГР_18_01_2025_стр27кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 28

Процедура СоздатьПоказателиСтроки28(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр28кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр28кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр28кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр28кол10(ВыводитьСообщения);

КонецПроцедуры

// Назначение: Создание показателя строка 2 колонка 3 (остаток исключительных программ на начало)
// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр28кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр28кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60901.ПС Субконто1. Вид НМА (НФО) «Исключительные (исследовательские) программы» на начало текущего периода";
	
	#endregion
	
	#region indicator_creation
	
	//todo уточнить, какой справочник в реальной конфигурации Хомнет

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|ДатаОстатка=НачалоДня(мНачалоПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр28кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр28кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» на начало текущего периода";
	
	#endregion
	
	#region indicator_creation
	
	//todo уточнить, какой справочник в реальной конфигурации Хомнет

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|ДатаОстатка=НачалоДня(мНачалоПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр28кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр28кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Прочие» на начало текущего периода";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|ДатаОстатка=НачалоДня(мНачалоПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр28кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр28кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр28кол3,ГР_18_01_2025_стр28кол4,ГР_18_01_2025_стр28кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 29

Процедура СоздатьПоказателиСтроки29(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр29кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр29кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр29кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр29кол10(ВыводитьСообщения);

КонецПроцедуры

// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр29кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр29кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» на начало текущего периода";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|ДатаОстатка=НачалоДня(мНачалоПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр29кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр29кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» на начало текущего периода";
	
	#endregion
	
	#region indicator_creation
	
	//todo уточнить, какой справочник в реальной конфигурации Хомнет

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|ДатаОстатка=НачалоДня(мНачалоПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр29кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр29кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Прочие» на начало текущего периода";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|ДатаОстатка=НачалоДня(мНачалоПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр29кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр29кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр29кол3,ГР_18_01_2025_стр29кол4,ГР_18_01_2025_стр29кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 31

Процедура СоздатьПоказателиСтроки31(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр31кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр31кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр31кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр31кол10(ВыводитьСообщения);

КонецПроцедуры

// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр31кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр31кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по дебету по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» в отчетном периоде текущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|_НачалоПериода=НачалоДня(мНачалоПериода);
	|_КонецПериода = КонецДня(мКонецПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр31кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр31кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по дебету по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» в  отчетном периоде текущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|_НачалоПериода=НачалоДня(мНачалоПериода);
	|_КонецПериода = КонецДня(мКонецПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр31кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр31кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по дебету по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Прочие» в  отчетном периоде текущего года";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|_НачалоПериода=НачалоДня(мНачалоПериода);
	|_КонецПериода = КонецДня(мКонецПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр31кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр31кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр31кол3,ГР_18_01_2025_стр31кол4,ГР_18_01_2025_стр31кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 34

Процедура СоздатьПоказателиСтроки34(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр34кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр34кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр34кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр34кол10(ВыводитьСообщения);
	
КонецПроцедуры

Процедура СоздатьПоказатель_стр34кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр34кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр35кол3,ГР_18_01_2025_стр36кол3";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр34кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр34кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 9 + строка 10";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр35кол4,ГР_18_01_2025_стр36кол4";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр34кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр34кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 9 + строка 10";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр35кол5,ГР_18_01_2025_стр36кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр34кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр34кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр34кол3,ГР_18_01_2025_стр34кол4,ГР_18_01_2025_стр34кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 35

Процедура СоздатьПоказателиСтроки35(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр35кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр35кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр35кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр35кол10(ВыводитьСообщения);

КонецПроцедуры

// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр35кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр35кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по кредиту по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» в отчетном периоде текущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|_НачалоПериода=НачалоДня(мНачалоПериода);
	|_КонецПериода = КонецДня(мКонецПериода);
	|ЗначениеПоказателя = -1*ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр35кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр35кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по кредиту по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» в  отчетном периоде текущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|_НачалоПериода=НачалоДня(мНачалоПериода);
	|_КонецПериода = КонецДня(мКонецПериода);
	|ЗначениеПоказателя = -1*ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр35кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр35кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по кредиту по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Прочие» в  отчетном периоде текущего года";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|_НачалоПериода=НачалоДня(мНачалоПериода);
	|_КонецПериода = КонецДня(мКонецПериода);
	|ЗначениеПоказателя = -1*ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр35кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр35кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 3 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр35кол3,ГР_18_01_2025_стр35кол4,ГР_18_01_2025_стр35кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 36

Процедура СоздатьПоказателиСтроки36(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр36кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр36кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр36кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр36кол10(ВыводитьСообщения);

КонецПроцедуры

// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр36кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр36кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот дебету  по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» в отчетном периоде текущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|_НачалоПериода=НачалоДня(мНачалоПериода);
	|_КонецПериода = КонецДня(мКонецПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр36кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр36кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот дебету  по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» в отчетном периоде текущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|_НачалоПериода=НачалоДня(мНачалоПериода);
	|_КонецПериода = КонецДня(мКонецПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр36кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр36кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот дебету  по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Прочие» в отчетном периоде текущего года";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=Истина;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|_НачалоПериода=НачалоДня(мНачалоПериода);
	|_КонецПериода = КонецДня(мКонецПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр36кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр36кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр36кол3,ГР_18_01_2025_стр36кол4,ГР_18_01_2025_стр36кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 38

Процедура СоздатьПоказателиСтроки38(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр38кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр38кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр38кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр38кол10(ВыводитьСообщения);

КонецПроцедуры

// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр38кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр38кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот кредиту  по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» в отчетном периоде текущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|_НачалоПериода=НачалоДня(мНачалоПериода);
	|_КонецПериода = КонецДня(мКонецПериода);
	|ЗначениеПоказателя = -1*ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр38кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр38кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот кредиту  по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» в отчетном периоде текущего года";
	
	#endregion
	
	#region indicator_creation
	
	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|_НачалоПериода=НачалоДня(мНачалоПериода);
	|_КонецПериода = КонецДня(мКонецПериода);
	|ЗначениеПоказателя = -1*ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр38кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр38кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот кредиту  по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Прочие» в отчетном периоде текущего года";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоДебетовыйОборот=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|_НачалоПериода=НачалоДня(мНачалоПериода);
	|_КонецПериода = КонецДня(мКонецПериода);
	|ЗначениеПоказателя = -1*ОбъектОбработчика
	|	.ОборотыПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, _НачалоПериода, _КонецПериода, ЭтоДебетовыйОборот);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоДебетовыйОборот = ""+строка(ЭтоДебетовыйОборот));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""_НачалоПериода = ""+строка(_НачалоПериода));
	|Сообщить(Префикс+""_КонецПериода = ""+строка(_КонецПериода));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр38кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр38кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 3 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр38кол3,ГР_18_01_2025_стр38кол4,ГР_18_01_2025_стр38кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 49

Процедура СоздатьПоказателиСтроки49(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр49кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр49кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр49кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр49кол10(ВыводитьСообщения);
	
КонецПроцедуры

Процедура СоздатьПоказатель_стр49кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр49кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "";
	
	#endregion
	
	#region indicator_creation
	
	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр27кол3,ГР_18_01_2025_стр31кол3,ГР_18_01_2025_стр34кол3,ГР_18_01_2025_стр38кол3";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр49кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр49кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр27кол4,ГР_18_01_2025_стр31кол4,ГР_18_01_2025_стр34кол4,ГР_18_01_2025_стр38кол4";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр49кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр49кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр27кол5,ГР_18_01_2025_стр31кол5,ГР_18_01_2025_стр34кол5,ГР_18_01_2025_стр38кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр49кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр49кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр49кол3,ГР_18_01_2025_стр49кол4,ГР_18_01_2025_стр49кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 50

Процедура СоздатьПоказателиСтроки50(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр50кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр50кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр50кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр50кол10(ВыводитьСообщения);

КонецПроцедуры

// Назначение: Создание показателя строка 2 колонка 3 (остаток исключительных программ на начало)
// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр50кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр50кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60901.ПС Субконто1. Вид НМА (НФО) «Исключительные (исследовательские) программы» на конец текущего периода";
	
	#endregion
	
	#region indicator_creation
	//Остаток по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» на начало текущего периода
	
	//todo уточнить, какой справочник в реальной конфигурации Хомнет

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|ДатаОстатка=КонецДня(мКонецПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр50кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр50кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» на конец текущего периода";
	
	#endregion
	
	#region indicator_creation
	
	//todo уточнить, какой справочник в реальной конфигурации Хомнет

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|ДатаОстатка=КонецДня(мКонецПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр50кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр50кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60901.ПС  Субконто1. Вид НМА (НФО) «Прочие» на конец текущего периода";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60901.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|ДатаОстатка=КонецДня(мКонецПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр50кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр50кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Строка 24 кол 10 = столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр50кол3,ГР_18_01_2025_стр50кол4,ГР_18_01_2025_стр50кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

//////////  СТРОКА 51

Процедура СоздатьПоказателиСтроки51(ВыводитьСообщения=Истина)

	СоздатьПоказатель_стр51кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр51кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр51кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр51кол10(ВыводитьСообщения);

КонецПроцедуры

// Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений
Процедура СоздатьПоказатель_стр51кол3(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр51кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60903.ПС Субконто1. Вид НМА (НФО) «Исключительные (исследовательские) программы» на конец текущего периода";
	
	#endregion
	
	#region indicator_creation
	//Остаток по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Программное обеспечение» на начало текущего периода
	
	//todo уточнить, какой справочник в реальной конфигурации Хомнет

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_ПО();
	|ДатаОстатка=КонецДня(мКонецПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр51кол4(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр51кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Лицензии и франшизы» на конец текущего периода";
	
	#endregion
	
	#region indicator_creation
	
	//todo уточнить, какой справочник в реальной конфигурации Хомнет

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Лиц();
	|ДатаОстатка=КонецДня(мКонецПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода ,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр51кол5(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр51кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60903.ПС  Субконто1. Вид НМА (НФО) «Прочие» на конец текущего периода";
	
	#endregion
	
	#region indicator_creation

	ТекстКода = "
	|СсылкаОбработчика=Справочники.ДополнительныеОтчетыИОбработки
	|	.НайтиПоНаименованию(""Отчет18012025"",Истина);
    |ДополнительныеОтчетыИОбработки.ПодключитьВнешнююОбработку(СсылкаОбработчика);
    |ОбъектОбработчика=ДополнительныеОтчетыИОбработки.ОбъектВнешнейОбработки(СсылкаОбработчика);
	|
	|ЭтоОстатокНаНачало=ложь;
	|СчетСсылка=ПланыСчетов.Хозрасчетный.НайтиПоКоду(""60903.ПС"");
	|СубконтоСсылка=ОбъектОбработчика.НМА_Прочие();
	|ДатаОстатка=КонецДня(мКонецПериода);
	|ЗначениеПоказателя = ОбъектОбработчика
	|	.ОстатокПоСчету(СтруктураПараметров.Организация, СчетСсылка, СубконтоСсылка, ДатаОстатка, ЭтоОстатокНаНачало);
	|Сообщить(""--------------------------------------------"");
	|Префикс = ""ГР 18.01_2025, строка  колонка ::""; Сообщить(Префикс);
	|Сообщить(Префикс+""ЭтоОстатокНаНачало = ""+строка(ЭтоОстатокНаНачало));
	|Сообщить(Префикс+""СчетСсылка = ""+строка(СчетСсылка));
	|Сообщить(Префикс+""ДатаОстатка = ""+строка(ДатаОстатка));
	|Сообщить(Префикс+""СтруктураПараметров.Организация = ""+строка(СтруктураПараметров.Организация));
	|Сообщить(Префикс+""Количество субконто в массиве=""+строка(СубконтоСсылка.Количество()));
	|Сообщить(Префикс+""Значения субконто:"");
	|для сч = 0 по СубконтоСсылка.Количество()-1 Цикл
	|	Сообщить(Префикс+""      знч = ""+строка(СубконтоСсылка[сч]));
	|КонецЦикла;
	|Сообщить(Префикс+""ЗначениеПоказателя = ""+строка(ЗначениеПоказателя));
	|Сообщить(""--------------------------------------------"");";

	Формула = "";
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",           //СписокИспользуемыхПоказателей
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

Процедура СоздатьПоказатель_стр51кол10(Знач ВыводитьСообщения = Ложь)

	#region initialization
	
	Идентификатор = "ГР_18_01_2025_стр51кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= столбец 3 + столбец 4 + столбец 5 +столбец 6 + столбец 7 +столбец 8 + столбец 9";
	
	#endregion
	
	#region indicator_creation

	СписокИспользуемыхПоказателей = "ГР_18_01_2025_стр51кол3,ГР_18_01_2025_стр51кол4,ГР_18_01_2025_стр51кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,             //Отбор
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	#endregion

КонецПроцедуры

#КонецОбласти

