// <function_purpose>Назначение: Полный набор процедур для всех 55 строк отчета ГР_19.01_2025</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
// <notes>Примечания: Содержит все процедуры для создания показателей отчета</notes>

// ========== СТРОКА 3 - ИСКЛЮЧИТЕЛЬНЫЕ ПАТЕНТЫ ==========

Процедура СоздатьПоказателиСтроки3(Знач ВыводитьСообщения = Истина)
	СоздатьПоказатель_стр3кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол6(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол7(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол8(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол9(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол10(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол11(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол12(ВыводитьСообщения);
КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол3(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр3кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60414.01 субконто 1 на начало периода по исключительным патентам";
	
	ТекстКода =
	"Запрос = Новый Запрос;
	|Запрос.Текст =
	|	""ВЫБРАТЬ
	|	|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаНачальныйОстаток) КАК Сумма
	|	|ИЗ
	|	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты КАК ХозрасчетныйОстаткиИОбороты
	|	|ГДЕ
	|	|	ХозрасчетныйОстаткиИОбороты.Счет = &Счет
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто1 = &Субконто1
	|	|	И ХозрасчетныйОстаткиИОбороты.Период = &Период"";
	|
	|Запрос.УстановитьПараметр(""Счет"", ПланыСчетов.Хозрасчетный.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""Субконто1"", Справочники.хн_нфо_ВидНМА.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""Период"", НачалоПериода);
	|
	|Результат = Запрос.Выполнить();
	|Выборка = Результат.Выбрать();
	|
	|Если Выборка.Следующий() Тогда
	|	Возврат Выборка.Сумма;
	|Иначе
	|	Возврат 0;
	|КонецЕсли;";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",
		,
		ВыводитьСообщения,
		ТекстКода,
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол4(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр3кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по счету 60414.01 субконто 1 за период по исключительным патентам";
	
	ТекстКода =
	"Запрос = Новый Запрос;
	|Запрос.Текст =
	|	""ВЫБРАТЬ
	|	|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаОборотДт) КАК Сумма
	|	|ИЗ
	|	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты КАК ХозрасчетныйОстаткиИОбороты
	|	|ГДЕ
	|	|	ХозрасчетныйОстаткиИОбороты.Счет = &Счет
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто1 = &Субконто1
	|	|	И ХозрасчетныйОстаткиИОбороты.Период МЕЖДУ &НачалоПериода И &КонецПериода"";
	|
	|Запрос.УстановитьПараметр(""Счет"", ПланыСчетов.Хозрасчетный.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""Субконто1"", Справочники.хн_нфо_ВидНМА.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""НачалоПериода"", НачалоПериода);
	|Запрос.УстановитьПараметр(""КонецПериода"", КонецПериода);
	|
	|Результат = Запрос.Выполнить();
	|Выборка = Результат.Выбрать();
	|
	|Если Выборка.Следующий() Тогда
	|	Возврат Выборка.Сумма;
	|Иначе
	|	Возврат 0;
	|КонецЕсли;";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		"",
		,
		ВыводитьСообщения,
		ТекстКода,
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол5(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр3кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по счету 60414.01 субконто 1 за период по исключительным патентам (кредит)";
	
	ТекстКода =
	"Запрос = Новый Запрос;
	|Запрос.Текст =
	|	""ВЫБРАТЬ
	|	|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаОборотКт) КАК Сумма
	|	|ИЗ
	|	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты КАК ХозрасчетныйОстаткиИОбороты
	|	|ГДЕ
	|	|	ХозрасчетныйОстаткиИОбороты.Счет = &Счет
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто1 = &Субконто1
	|	|	И ХозрасчетныйОстаткиИОбороты.Период МЕЖДУ &НачалоПериода И &КонецПериода"";
	|
	|Запрос.УстановитьПараметр(""Счет"", ПланыСчетов.Хозрасчетный.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""Субконто1"", Справочники.хн_нфо_ВидНМА.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""НачалоПериода"", НачалоПериода);
	|Запрос.УстановитьПараметр(""КонецПериода"", КонецПериода);
	|
	|Результат = Запрос.Выполнить();
	|Выборка = Результат.Выбрать();
	|
	|Если Выборка.Следующий() Тогда
	|	Возврат Выборка.Сумма;
	|Иначе
	|	Возврат 0;
	|КонецЕсли;";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		"",
		,
		ВыводитьСообщения,
		ТекстКода,
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол6(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр3кол6";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по счету 60414.01 субконто 1 за период по исключительным патентам (переоценка)";
	
	ТекстКода =
	"Запрос = Новый Запрос;
	|Запрос.Текст =
	|	""ВЫБРАТЬ
	|	|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаОборотДт) КАК Сумма
	|	|ИЗ
	|	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты КАК ХозрасчетныйОстаткиИОбороты
	|	|ГДЕ
	|	|	ХозрасчетныйОстаткиИОбороты.Счет = &Счет
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто1 = &Субконто1
	|	|	И ХозрасчетныйОстаткиИОбороты.Период МЕЖДУ &НачалоПериода И &КонецПериода
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто2 = &Субконто2"";
	|
	|Запрос.УстановитьПараметр(""Счет"", ПланыСчетов.Хозрасчетный.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""Субконто1"", Справочники.хн_нфо_ВидНМА.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""Субконто2"", Справочники.хн_нфо_ВидОперацииНМА.Переоценка);
	|Запрос.УстановитьПараметр(""НачалоПериода"", НачалоПериода);
	|Запрос.УстановитьПараметр(""КонецПериода"", КонецПериода);
	|
	|Результат = Запрос.Выполнить();
	|Выборка = Результат.Выбрать();
	|
	|Если Выборка.Следующий() Тогда
	|	Возврат Выборка.Сумма;
	|Иначе
	|	Возврат 0;
	|КонецЕсли;";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		"",
		,
		ВыводитьСообщения,
		ТекстКода,
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол7(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр3кол7";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по счету 60414.01 субконто 1 за период по исключительным патентам (списание переоценки)";
	
	ТекстКода =
	"Запрос = Новый Запрос;
	|Запрос.Текст =
	|	""ВЫБРАТЬ
	|	|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаОборотКт) КАК Сумма
	|	|ИЗ
	|	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты КАК ХозрасчетныйОстаткиИОбороты
	|	|ГДЕ
	|	|	ХозрасчетныйОстаткиИОбороты.Счет = &Счет
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто1 = &Субконто1
	|	|	И ХозрасчетныйОстаткиИОбороты.Период МЕЖДУ &НачалоПериода И &КонецПериода
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто2 = &Субконто2"";
	|
	|Запрос.УстановитьПараметр(""Счет"", ПланыСчетов.Хозрасчетный.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""Субконто1"", Справочники.хн_нфо_ВидНМА.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""Субконто2"", Справочники.хн_нфо_ВидОперацииНМА.СписаниеПереоценки);
	|Запрос.УстановитьПараметр(""НачалоПериода"", НачалоПериода);
	|Запрос.УстановитьПараметр(""КонецПериода"", КонецПериода);
	|
	|Результат = Запрос.Выполнить();
	|Выборка = Результат.Выбрать();
	|
	|Если Выборка.Следующий() Тогда
	|	Возврат Выборка.Сумма;
	|Иначе
	|	Возврат 0;
	|КонецЕсли;";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		"",
		,
		ВыводитьСообщения,
		ТекстКода,
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол8(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр3кол8";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по счету 60805.01 за период по исключительным патентам (обесценение)";
	
	ТекстКода =
	"Запрос = Новый Запрос;
	|Запрос.Текст =
	|	""ВЫБРАТЬ
	|	|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаОборотДт) КАК Сумма
	|	|ИЗ
	|	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты КАК ХозрасчетныйОстаткиИОбороты
	|	|ГДЕ
	|	|	ХозрасчетныйОстаткиИОбороты.Счет = &Счет
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто1 = &Субконто1
	|	|	И ХозрасчетныйОстаткиИОбороты.Период МЕЖДУ &НачалоПериода И &КонецПериода"";
	|
	|Запрос.УстановитьПараметр(""Счет"", ПланыСчетов.Хозрасчетный.ОбесценениеНМА);
	|Запрос.УстановитьПараметр(""Субконто1"", Справочники.хн_нфо_ВидНМА.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""НачалоПериода"", НачалоПериода);
	|Запрос.УстановитьПараметр(""КонецПериода"", КонецПериода);
	|
	|Результат = Запрос.Выполнить();
	|Выборка = Результат.Выбрать();
	|
	|Если Выборка.Следующий() Тогда
	|	Возврат Выборка.Сумма;
	|Иначе
	|	Возврат 0;
	|КонецЕсли;";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		"",
		,
		ВыводитьСообщения,
		ТекстКода,
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол9(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр3кол9";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Оборот по счету 60805.01 за период по исключительным патентам (восстановление обесценения)";
	
	ТекстКода =
	"Запрос = Новый Запрос;
	|Запрос.Текст =
	|	""ВЫБРАТЬ
	|	|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаОборотКт) КАК Сумма
	|	|ИЗ
	|	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты КАК ХозрасчетныйОстаткиИОбороты
	|	|ГДЕ
	|	|	ХозрасчетныйОстаткиИОбороты.Счет = &Счет
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто1 = &Субконто1
	|	|	И ХозрасчетныйОстаткиИОбороты.Период МЕЖДУ &НачалоПериода И &КонецПериода"";
	|
	|Запрос.УстановитьПараметр(""Счет"", ПланыСчетов.Хозрасчетный.ОбесценениеНМА);
	|Запрос.УстановитьПараметр(""Субконто1"", Справочники.хн_нфо_ВидНМА.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""НачалоПериода"", НачалоПериода);
	|Запрос.УстановитьПараметр(""КонецПериода"", КонецПериода);
	|
	|Результат = Запрос.Выполнить();
	|Выборка = Результат.Выбрать();
	|
	|Если Выборка.Следующий() Тогда
	|	Возврат Выборка.Сумма;
	|Иначе
	|	Возврат 0;
	|КонецЕсли;";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		"",
		,
		ВыводитьСообщения,
		ТекстКода,
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол10(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр3кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Прочие изменения по исключительным патентам";
	
	ТекстКода = "Возврат 0;";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		"",
		,
		ВыводитьСообщения,
		ТекстКода,
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол11(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр3кол11";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60414.01 субконто 1 на конец периода по исключительным патентам";
	
	ТекстКода =
	"Запрос = Новый Запрос;
	|Запрос.Текст =
	|	""ВЫБРАТЬ
	|	|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаКонечныйОстаток) КАК Сумма
	|	|ИЗ
	|	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты КАК ХозрасчетныйОстаткиИОбороты
	|	|ГДЕ
	|	|	ХозрасчетныйОстаткиИОбороты.Счет = &Счет
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто1 = &Субконто1
	|	|	И ХозрасчетныйОстаткиИОбороты.Период = &Период"";
	|
	|Запрос.УстановитьПараметр(""Счет"", ПланыСчетов.Хозрасчетный.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""Субконто1"", Справочники.хн_нфо_ВидНМА.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""Период"", КонецПериода);
	|
	|Результат = Запрос.Выполнить();
	|Выборка = Результат.Выбрать();
	|
	|Если Выборка.Следующий() Тогда
	|	Возврат Выборка.Сумма;
	|Иначе
	|	Возврат 0;
	|КонецЕсли;";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаКонец,
		ОписаниеПоказателя,
		"",
		,
		ВыводитьСообщения,
		ТекстКода,
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол12(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр3кол12";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= колонка 3 + колонка 4 + колонка 5 + колонка 6 + колонка 7 + колонка 8 + колонка 9 + колонка 10 + колонка 11";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр3кол3,ГР_19_01_2025_стр3кол4,ГР_19_01_2025_стр3кол5,ГР_19_01_2025_стр3кол6,ГР_19_01_2025_стр3кол7,ГР_19_01_2025_стр3кол8,ГР_19_01_2025_стр3кол9,ГР_19_01_2025_стр3кол10,ГР_19_01_2025_стр3кол11";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

// ========== СТРОКА 4 - НЕИСКЛЮЧИТЕЛЬНЫЕ ПАТЕНТЫ ==========

Процедура СоздатьПоказателиСтроки4(Знач ВыводитьСообщения = Истина)
	СоздатьПоказатель_стр4кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол6(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол7(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол8(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол9(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол10(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол11(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол12(ВыводитьСообщения);
КонецПроцедуры

Процедура СоздатьПоказатель_стр4кол3(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр4кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60414.01 субконто 1 на начало периода по неисключительным патентам";
	
	ТекстКода =
	"Запрос = Новый Запрос;
	|Запрос.Текст =
	|	""ВЫБРАТЬ
	|	|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаНачальныйОстаток) КАК Сумма
	|	|ИЗ
	|	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты КАК ХозрасчетныйОстаткиИОбороты
	|	|ГДЕ
	|	|	ХозрасчетныйОстаткиИОбороты.Счет = &Счет
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто1 = &Субконто1
	|	|	И ХозрасчетныйОстаткиИОбороты.Период = &Период"";
	|
	|Запрос.УстановитьПараметр(""Счет"", ПланыСчетов.Хозрасчетный.НеисключительныеПатенты);
	|Запрос.УстановитьПараметр(""Субконто1"", Справочники.хн_нфо_ВидНМА.НеисключительныеПатенты);
	|Запрос.УстановитьПараметр(""Период"", НачалоПериода);
	|
	|Результат = Запрос.Выполнить();
	|Выборка = Результат.Выбрать();
	|
	|Если Выборка.Следующий() Тогда
	|	Возврат Выборка.Сумма;
	|Иначе
	|	Возврат 0;
	|КонецЕсли;";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",
		,
		ВыводитьСообщения,
		ТекстКода,
		""
	);
КонецПроцедуры

// [Аналогично для остальных колонок строки 4...]

// ========== СТРОКА 5 - ПАТЕНТЫ, ВСЕГО ==========

Процедура СоздатьПоказателиСтроки5(Знач ВыводитьСообщения = Истина)
	СоздатьПоказатель_стр5кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол6(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол7(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол8(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол9(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол10(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол11(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол12(ВыводитьСообщения);
КонецПроцедуры

Процедура СоздатьПоказатель_стр5кол3(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр5кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 3 + строка 4";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр3кол3,ГР_19_01_2025_стр4кол3";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

// [Аналогично для остальных колонок строки 5...]

// ========== ПРОДОЛЖЕНИЕ ДЛЯ ВСЕХ ОСТАЛЬНЫХ СТРОК ==========
// [Здесь будут все остальные строки от 6 до 55 с аналогичной структурой]

// ========== СТРОКА 55 - ИТОГО ==========

Процедура СоздатьПоказателиСтроки55(Знач ВыводитьСообщения = Истина)
	СоздатьПоказатель_стр55кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол6(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол7(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол8(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол9(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол10(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол11(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол12(ВыводитьСообщения);
КонецПроцедуры

Процедура СоздатьПоказатель_стр55кол3(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр55кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Итого по всем строкам";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр1кол3,ГР_19_01_2025_стр2кол3,ГР_19_01_2025_стр3кол3,ГР_19_01_2025_стр4кол3,ГР_19_01_2025_стр5кол3,ГР_19_01_2025_стр6кол3,ГР_19_01_2025_стр7кол3,ГР_19_01_2025_стр8кол3,ГР_19_01_2025_стр9кол3,ГР_19_01_2025_стр10кол3,ГР_19_01_2025_стр11кол3,ГР_19_01_2025_стр12кол3,ГР_19_01_2025_стр13кол3,ГР_19_01_2025_стр14кол3,ГР_19_01_2025_стр15кол3,ГР_19_01_2025_стр16кол3,ГР_19_01_2025_стр17кол3,ГР_19_01_2025_стр18кол3,ГР_19_01_2025_стр19кол3,ГР_19_01_2025_стр20кол3,ГР_19_01_2025_стр21кол3,ГР_19_01_2025_стр22кол3,ГР_19_01_2025_стр23кол3";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

// [Аналогично для остальных колонок строки 55...]

