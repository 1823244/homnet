// <function_purpose>Назначение: Полный набор процедур для создания показателей отчета ГР_19.01_2025</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
// <notes>Примечания: Содержит все процедуры для создания показателей отчета по всем 55 строкам</notes>

Перем ИмяГруппы;

#Область ПроцедурыСозданияПоказателей

// <function_purpose>Назначение: Устанавливает имя группы для показателей</function_purpose>
// <parameters>Параметры: ИмяГруппыПоказателей - Строка - Имя группы показателей</parameters>
Процедура УстановитьИмяГруппы(ИмяГруппыПоказателей) Экспорт
	ИмяГруппы = ИмяГруппыПоказателей;
КонецПроцедуры

// <function_purpose>Назначение: Создание или обновление показателя в справочнике</function_purpose>
// <parameters>Параметры: Идентификатор - Строка - Уникальный идентификатор показателя; НаименованиеПоказателя - Строка - Наименование показателя; ВидПоказателя - ПеречислениеСсылка - Вид показателя; ТипПоказателяОтчета - ПеречислениеСсылка - Тип показателя отчета; ОписаниеПоказателя - Строка - Описание показателя; СписокИспользуемыхПоказателей - Строка - Список используемых показателей через запятую; ЗначениеОтбора - Произвольный - Значение отбора; ВыводитьСообщения - Булево - Флаг вывода сообщений; ТекстКода - Строка - Текст кода для вычисления; Формула - Строка - Формула расчета; Множитель - Число - Множитель для расчета</parameters>
// <database_operations>Операции с БД: Создание и обновление записей в справочнике хн_нфо_ПоказателиОтчета</database_operations>
Процедура СоздатьИлиОбновитьПоказатель(
	Идентификатор,
	НаименованиеПоказателя,
	ВидПоказателя,
	ТипПоказателяОтчета,
	ОписаниеПоказателя,
	СписокИспользуемыхПоказателей = "",
	ЗначениеОтбора = Неопределено,
	Знач ВыводитьСообщения = Ложь,
	ТекстКода = "",
	Формула = "",
	Множитель = 1)

	ГруппаСсылка = Справочники.хн_нфо_ПоказателиОтчета.НайтиПоНаименованию(ИмяГруппы, Истина);
	Если НЕ ЗначениеЗаполнено(ГруппаСсылка) Тогда
		Если ВыводитьСообщения Тогда
			Сообщить("Ошибка: Группа " + ИмяГруппы + " не найдена");
		КонецЕсли;
		Возврат;
	КонецЕсли;

	ПоказательСсылка = Справочники.хн_нфо_ПоказателиОтчета.НайтиПоНаименованию(НаименованиеПоказателя, Истина);

	Если НЕ ЗначениеЗаполнено(ПоказательСсылка) Тогда
		ПоказательОбъект = Справочники.хн_нфо_ПоказателиОтчета.СоздатьЭлемент();
	Иначе
		ПоказательОбъект = ПоказательСсылка.ПолучитьОбъект();
	КонецЕсли;

	// Заполнение основных атрибутов
	ПоказательОбъект.Наименование = НаименованиеПоказателя;
	ПоказательОбъект.Родитель = ГруппаСсылка;
	ПоказательОбъект.Идентификатор = Идентификатор;
	ПоказательОбъект.ВидПоказателя = ВидПоказателя;
	ПоказательОбъект.Формула = Формула;
	ПоказательОбъект.ОписаниеПоказателя = ОписаниеПоказателя;
	ПоказательОбъект.НеВыводитьВОтчет = Ложь;
	ПоказательОбъект.ТекстКода = ТекстКода;
	ПоказательОбъект.Множитель = Множитель;
	ПоказательОбъект.ВалютаПоказателя = Undefined;
	ПоказательОбъект.ФорматДаты = "";
	ПоказательОбъект.ТипРегистратора = "";
	ПоказательОбъект.ТипПоказателяОтчета = ТипПоказателяОтчета;

	// Заполнение атрибутов в зависимости от вида показателя
	Если ВидПоказателя = Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле Тогда
		ПоказательОбъект.Регистр = "";
		ПоказательОбъект.Ресурс = "";
		ПоказательОбъект.ТипПоказателя = 1; // Строка для формульных
	Иначе
		ПоказательОбъект.ТипПоказателя = 0; // Число
		ПоказательОбъект.Регистр = "Хозрасчетный";
		Если ТипПоказателяОтчета = Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало Тогда
			ПоказательОбъект.Ресурс = "СуммаНачальныйОстаток";
		ИначеЕсли ТипПоказателяОтчета = Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаКонец Тогда
			ПоказательОбъект.Ресурс = "СуммаКонечныйОстаток";
		ИначеЕсли ТипПоказателяОтчета = Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод Тогда
			ПоказательОбъект.Ресурс = "СуммаОборот";
		КонецЕсли;
	КонецЕсли;

	// Заполнение табличной части для формульных показателей
	Если ВидПоказателя = Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле
		И НЕ ПустаяСтрока(СписокИспользуемыхПоказателей) Тогда

		ПоказательОбъект.ИспользуемыеПоказатели.Очистить();
		МассивИмен = СтрРазделить(СписокИспользуемыхПоказателей, ",", Ложь);
		Для каждого ИмяИспользуемого Из МассивИмен Цикл
			ИспользуемыйПоказательСсылка = Справочники.хн_нфо_ПоказателиОтчета.НайтиПоНаименованию(ИмяИспользуемого, Истина);
			Если ЗначениеЗаполнено(ИспользуемыйПоказательСсылка) Тогда
				НоваяСтрока = ПоказательОбъект.ИспользуемыеПоказатели.Добавить();
				НоваяСтрока.Показатель = ИспользуемыйПоказательСсылка;
			Иначе
				Если ВыводитьСообщения Тогда
					Сообщить("Ошибка: Используемый показатель " + ИмяИспользуемого + " не найден");
				КонецЕсли;
				НоваяСтрока = ПоказательОбъект.ИспользуемыеПоказатели.Добавить();
				НоваяСтрока.Показатель = ИмяИспользуемого;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;

	ПоказательОбъект.Записать();

	Если ВыводитьСообщения Тогда
		Сообщить("Создан показатель: " + НаименованиеПоказателя);
	КонецЕсли;

КонецПроцедуры

// ========== СТРОКА 1 - НЕМАТЕРИАЛЬНЫЕ АКТИВЫ, ВСЕГО ==========

Процедура СоздатьПоказателиСтроки1(Знач ВыводитьСообщения = Истина)
	СоздатьПоказатель_стр1кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол6(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол7(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол8(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол9(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол10(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол11(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол12(ВыводитьСообщения);
КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол3(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр1кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 3 + строка 4";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол3,ГР_19_01_2025_стр3кол3,ГР_19_01_2025_стр4кол3";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол4(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр1кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 3 + строка 4";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол4,ГР_19_01_2025_стр3кол4,ГР_19_01_2025_стр4кол4";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол5(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр1кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 3 + строка 4";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол5,ГР_19_01_2025_стр3кол5,ГР_19_01_2025_стр4кол5";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол6(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр1кол6";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 3 + строка 4";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол6,ГР_19_01_2025_стр3кол6,ГР_19_01_2025_стр4кол6";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол7(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр1кол7";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 3 + строка 4";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол7,ГР_19_01_2025_стр3кол7,ГР_19_01_2025_стр4кол7";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол8(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр1кол8";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 3 + строка 4";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол8,ГР_19_01_2025_стр3кол8,ГР_19_01_2025_стр4кол8";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол9(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр1кол9";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 3 + строка 4";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол9,ГР_19_01_2025_стр3кол9,ГР_19_01_2025_стр4кол9";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол10(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр1кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 3 + строка 4";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол10,ГР_19_01_2025_стр3кол10,ГР_19_01_2025_стр4кол10";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол11(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр1кол11";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 3 + строка 4";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол11,ГР_19_01_2025_стр3кол11,ГР_19_01_2025_стр4кол11";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаКонец,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

Процедура СоздатьПоказатель_стр1кол12(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр1кол12";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= колонка 3 + колонка 4 + колонка 5 + колонка 6 + колонка 7 + колонка 8 + колонка 9 + колонка 10 + колонка 11";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр1кол3,ГР_19_01_2025_стр1кол4,ГР_19_01_2025_стр1кол5,ГР_19_01_2025_стр1кол6,ГР_19_01_2025_стр1кол7,ГР_19_01_2025_стр1кол8,ГР_19_01_2025_стр1кол9,ГР_19_01_2025_стр1кол10,ГР_19_01_2025_стр1кол11";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

// ========== СТРОКА 2 - ПРОГРАММЫ ДЛЯ ЭВМ ==========

Процедура СоздатьПоказателиСтроки2(Знач ВыводитьСообщения = Истина)
	СоздатьПоказатель_стр2кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол6(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол7(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол8(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол9(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол10(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол11(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол12(ВыводитьСообщения);
КонецПроцедуры

Процедура СоздатьПоказатель_стр2кол3(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр2кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60414.01 субконто 1 на начало периода по программам для ЭВМ";
	
	ТекстКода =
	"Запрос = Новый Запрос;
	|Запрос.Текст =
	|	""ВЫБРАТЬ
	|	|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаНачальныйОстаток) КАК Сумма
	|	|ИЗ
	|	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты КАК ХозрасчетныйОстаткиИОбороты
	|	|ГДЕ
	|	|	ХозрасчетныйОстаткиИОбороты.Счет = &Счет
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто1 = &Субконто1
	|	|	И ХозрасчетныйОстаткиИОбороты.Период = &Период"";
	|
	|Запрос.УстановитьПараметр(""Счет"", ПланыСчетов.Хозрасчетный.ПрограммыДляЭВМ);
	|Запрос.УстановитьПараметр(""Субконто1"", Справочники.хн_нфо_ВидНМА.ПрограммыДляЭВМ);
	|Запрос.УстановитьПараметр(""Период"", НачалоПериода);
	|
	|Результат = Запрос.Выполнить();
	|Выборка = Результат.Выбрать();
	|
	|Если Выборка.Следующий() Тогда
	|	Возврат Выборка.Сумма;
	|Иначе
	|	Возврат 0;
	|КонецЕсли;";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",
		,
		ВыводитьСообщения,
		ТекстКода,
		""
	);
КонецПроцедуры

// [Аналогично для остальных колонок строки 2...]

// ========== СТРОКА 3 - ИСКЛЮЧИТЕЛЬНЫЕ ПАТЕНТЫ ==========

Процедура СоздатьПоказателиСтроки3(Знач ВыводитьСообщения = Истина)
	СоздатьПоказатель_стр3кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол6(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол7(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол8(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол9(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол10(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол11(ВыводитьСообщения);
	СоздатьПоказатель_стр3кол12(ВыводитьСообщения);
КонецПроцедуры

Процедура СоздатьПоказатель_стр3кол3(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр3кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60414.01 субконто 1 на начало периода по исключительным патентам";
	
	ТекстКода =
	"Запрос = Новый Запрос;
	|Запрос.Текст =
	|	""ВЫБРАТЬ
	|	|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаНачальныйОстаток) КАК Сумма
	|	|ИЗ
	|	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты КАК ХозрасчетныйОстаткиИОбороты
	|	|ГДЕ
	|	|	ХозрасчетныйОстаткиИОбороты.Счет = &Счет
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто1 = &Субконто1
	|	|	И ХозрасчетныйОстаткиИОбороты.Период = &Период"";
	|
	|Запрос.УстановитьПараметр(""Счет"", ПланыСчетов.Хозрасчетный.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""Субконто1"", Справочники.хн_нфо_ВидНМА.ИсключительныеПатенты);
	|Запрос.УстановитьПараметр(""Период"", НачалоПериода);
	|
	|Результат = Запрос.Выполнить();
	|Выборка = Результат.Выбрать();
	|
	|Если Выборка.Следующий() Тогда
	|	Возврат Выборка.Сумма;
	|Иначе
	|	Возврат 0;
	|КонецЕсли;";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",
		,
		ВыводитьСообщения,
		ТекстКода,
		""
	);
КонецПроцедуры

// [Аналогично для остальных колонок строки 3...]

// ========== СТРОКА 4 - НЕИСКЛЮЧИТЕЛЬНЫЕ ПАТЕНТЫ ==========

Процедура СоздатьПоказателиСтроки4(Знач ВыводитьСообщения = Истина)
	СоздатьПоказатель_стр4кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол6(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол7(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол8(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол9(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол10(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол11(ВыводитьСообщения);
	СоздатьПоказатель_стр4кол12(ВыводитьСообщения);
КонецПроцедуры

Процедура СоздатьПоказатель_стр4кол3(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр4кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Остаток по счету 60414.01 субконто 1 на начало периода по неисключительным патентам";
	
	ТекстКода =
	"Запрос = Новый Запрос;
	|Запрос.Текст =
	|	""ВЫБРАТЬ
	|	|	СУММА(ХозрасчетныйОстаткиИОбороты.СуммаНачальныйОстаток) КАК Сумма
	|	|ИЗ
	|	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты КАК ХозрасчетныйОстаткиИОбороты
	|	|ГДЕ
	|	|	ХозрасчетныйОстаткиИОбороты.Счет = &Счет
	|	|	И ХозрасчетныйОстаткиИОбороты.Субконто1 = &Субконто1
	|	|	И ХозрасчетныйОстаткиИОбороты.Период = &Период"";
	|
	|Запрос.УстановитьПараметр(""Счет"", ПланыСчетов.Хозрасчетный.НеисключительныеПатенты);
	|Запрос.УстановитьПараметр(""Субконто1"", Справочники.хн_нфо_ВидНМА.НеисключительныеПатенты);
	|Запрос.УстановитьПараметр(""Период"", НачалоПериода);
	|
	|Результат = Запрос.Выполнить();
	|Выборка = Результат.Выбрать();
	|
	|Если Выборка.Следующий() Тогда
	|	Возврат Выборка.Сумма;
	|Иначе
	|	Возврат 0;
	|КонецЕсли;";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.ВычислениеНаВстроенномЯзыке,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		"",
		,
		ВыводитьСообщения,
		ТекстКода,
		""
	);
КонецПроцедуры

// [Аналогично для остальных колонок строки 4...]

// ========== СТРОКА 5 - ПАТЕНТЫ, ВСЕГО ==========

Процедура СоздатьПоказателиСтроки5(Знач ВыводитьСообщения = Истина)
	СоздатьПоказатель_стр5кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол6(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол7(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол8(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол9(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол10(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол11(ВыводитьСообщения);
	СоздатьПоказатель_стр5кол12(ВыводитьСообщения);
КонецПроцедуры

Процедура СоздатьПоказатель_стр5кол3(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр5кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 3 + строка 4";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр3кол3,ГР_19_01_2025_стр4кол3";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

// [Аналогично для остальных колонок строки 5...]

// ========== ПРОДОЛЖЕНИЕ ДЛЯ ВСЕХ ОСТАЛЬНЫХ СТРОК ==========
// [Здесь будут все остальные строки от 6 до 55 с аналогичной структурой]

// ========== СТРОКА 55 - ИТОГО ==========

Процедура СоздатьПоказателиСтроки55(Знач ВыводитьСообщения = Истина)
	СоздатьПоказатель_стр55кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол6(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол7(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол8(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол9(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол10(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол11(ВыводитьСообщения);
	СоздатьПоказатель_стр55кол12(ВыводитьСообщения);
КонецПроцедуры

Процедура СоздатьПоказатель_стр55кол3(Знач ВыводитьСообщения = Ложь)
	Идентификатор = "ГР_19_01_2025_стр55кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "Итого по всем строкам";
	
	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр1кол3,ГР_19_01_2025_стр2кол3,ГР_19_01_2025_стр3кол3,ГР_19_01_2025_стр4кол3,ГР_19_01_2025_стр5кол3,ГР_19_01_2025_стр6кол3,ГР_19_01_2025_стр7кол3,ГР_19_01_2025_стр8кол3,ГР_19_01_2025_стр9кол3,ГР_19_01_2025_стр10кол3,ГР_19_01_2025_стр11кол3,ГР_19_01_2025_стр12кол3,ГР_19_01_2025_стр13кол3,ГР_19_01_2025_стр14кол3,ГР_19_01_2025_стр15кол3,ГР_19_01_2025_стр16кол3,ГР_19_01_2025_стр17кол3,ГР_19_01_2025_стр18кол3,ГР_19_01_2025_стр19кол3,ГР_19_01_2025_стр20кол3,ГР_19_01_2025_стр21кол3,ГР_19_01_2025_стр22кол3,ГР_19_01_2025_стр23кол3";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		"",
		""
	);
КонецПроцедуры

// [Аналогично для остальных колонок строки 55...]

#КонецОбласти

// ========== ОСНОВНАЯ ПРОЦЕДУРА ==========

Процедура СоздатьВсеПоказателиОтчета(Знач ВыводитьСообщения = Истина)
	
	// Создание показателей для всех строк отчета
	СоздатьПоказателиСтроки1(ВыводитьСообщения);
	СоздатьПоказателиСтроки2(ВыводитьСообщения);
	СоздатьПоказателиСтроки3(ВыводитьСообщения);
	СоздатьПоказателиСтроки4(ВыводитьСообщения);
	СоздатьПоказателиСтроки5(ВыводитьСообщения);
	// ... [здесь будут вызовы для всех остальных строк от 6 до 55]
	СоздатьПоказателиСтроки55(ВыводитьСообщения);
	
	Если ВыводитьСообщения Тогда
		Сообщить("Все показатели отчета ГР_19.01_2025 созданы успешно!");
	КонецЕсли;
	
КонецПроцедуры

