// <function_purpose>Назначение: Создание показателей отчета ГР_19.01_2025 в справочнике хн_нфо_ПоказателиОтчета</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
// <notes>Примечания: Создает все показатели для отчета согласно техническому заданию</notes>
// <database_operations>Операции с БД: Создание и обновление записей в справочнике хн_нфо_ПоказателиОтчета</database_operations>

Перем ИмяГруппы;

// <function_purpose>Назначение: Создание всех показателей отчета ГР_19.01_2025</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьВсеПоказателиОтчета(Знач ВыводитьСообщения = Истина)

	<initialization>
	
	ИмяГруппы = "ГР_19.01_2025";
	
	Если ВыводитьСообщения Тогда
		Сообщить("<operation_start>Начало создания показателей отчета ГР_19.01_2025</operation_start>");
	КонецЕсли;
	
	</initialization>
	
	<data_processing>
	
	// Создание показателей для всех строк таблицы
	СоздатьПоказателиБазовыхСтрок(ВыводитьСообщения);
	СоздатьПоказателиИтоговыхСтрок(ВыводитьСообщения);
	
	</data_processing>
	
	<cleanup>
	
	Если ВыводитьСообщения Тогда
		Сообщить("<operation_end>Завершение создания показателей отчета ГР_19.01_2025</operation_end>");
	КонецЕсли;
	
	</cleanup>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателей базовых строк отчета</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказателиБазовыхСтрок(Знач ВыводитьСообщения = Истина)

	<data_processing>
	
	// Строка 3 - Исключительные патенты
	СоздатьПоказателиСтроки3(ВыводитьСообщения);
	
	// Строка 4 - Неисключительные патенты  
	СоздатьПоказателиСтроки4(ВыводитьСообщения);
	
	// Строка 6 - Исключительные программы
	СоздатьПоказателиСтроки6(ВыводитьСообщения);
	
	// Строка 7 - Неисключительные программы
	СоздатьПоказателиСтроки7(ВыводитьСообщения);
	
	// Строка 11 - Исключительные изобретения
	СоздатьПоказателиСтроки11(ВыводитьСообщения);
	
	// Строка 12 - Неисключительные изобретения
	СоздатьПоказателиСтроки12(ВыводитьСообщения);
	
	// Строка 14 - Полезные модели
	СоздатьПоказателиСтроки14(ВыводитьСообщения);
	
	// Строка 16 - Промышленные образцы
	СоздатьПоказателиСтроки16(ВыводитьСообщения);
	
	// Строка 18 - Товарные знаки
	СоздатьПоказателиСтроки18(ВыводитьСообщения);
	
	// Строка 20 - Ноу-хау
	СоздатьПоказателиСтроки20(ВыводитьСообщения);
	
	// Строка 22 - Деловая репутация
	СоздатьПоказателиСтроки22(ВыводитьСообщения);
	
	</data_processing>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателей итоговых строк отчета</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказателиИтоговыхСтрок(Знач ВыводитьСообщения = Истина)

	<data_processing>
	
	// Строка 1 - Нематериальные активы, всего
	СоздатьПоказателиСтроки1(ВыводитьСообщения);
	
	// Строка 2 - Объекты интеллектуальной собственности
	СоздатьПоказателиСтроки2(ВыводитьСообщения);
	
	// Строка 5 - Патенты, всего
	СоздатьПоказателиСтроки5(ВыводитьСообщения);
	
	// Строка 8 - Программы, всего
	СоздатьПоказателиСтроки8(ВыводитьСообщения);
	
	// Строка 9 - Объекты авторского права
	СоздатьПоказателиСтроки9(ВыводитьСообщения);
	
	// Строка 10 - Объекты смежных прав
	СоздатьПоказателиСтроки10(ВыводитьСообщения);
	
	// Строка 13 - Изобретения, всего
	СоздатьПоказателиСтроки13(ВыводитьСообщения);
	
	// Строка 15 - Полезные модели, всего
	СоздатьПоказателиСтроки15(ВыводитьСообщения);
	
	// Строка 17 - Промышленные образцы, всего
	СоздатьПоказателиСтроки17(ВыводитьСообщения);
	
	// Строка 19 - Товарные знаки, всего
	СоздатьПоказателиСтроки19(ВыводитьСообщения);
	
	// Строка 21 - Ноу-хау, всего
	СоздатьПоказателиСтроки21(ВыводитьСообщения);
	
	// Строка 23 - Деловая репутация, всего
	СоздатьПоказателиСтроки23(ВыводитьСообщения);
	
	</data_processing>

КонецПроцедуры

// <function_purpose>Назначение: Создание или обновление показателя в справочнике</function_purpose>
// <parameters>Параметры: Идентификатор - Строка - Уникальный идентификатор показателя; НаименованиеПоказателя - Строка - Наименование показателя; ВидПоказателя - ПеречислениеСсылка - Вид показателя; ТипПоказателяОтчета - ПеречислениеСсылка - Тип показателя отчета; ОписаниеПоказателя - Строка - Описание показателя; СписокИспользуемыхПоказателей - Строка - Список используемых показателей через запятую; ЗначениеОтбора - Произвольный - Значение отбора; ВыводитьСообщения - Булево - Флаг вывода сообщений; ТекстКода - Строка - Текст кода для вычисления; Формула - Строка - Формула расчета; Множитель - Число - Множитель для расчета</parameters>
// <database_operations>Операции с БД: Создание и обновление записей в справочнике хн_нфо_ПоказателиОтчета</database_operations>
Процедура СоздатьИлиОбновитьПоказатель(
	Идентификатор,
	НаименованиеПоказателя,
	ВидПоказателя,
	ТипПоказателяОтчета,
	ОписаниеПоказателя,
	СписокИспользуемыхПоказателей = "",
	ЗначениеОтбора = Неопределено,
	Знач ВыводитьСообщения = Ложь,
	ТекстКода = "",
	Формула = "",
	Множитель = 1)
	
	<validation>
	
	ГруппаСсылка = Справочники.хн_нфо_ПоказателиОтчета.НайтиПоНаименованию(ИмяГруппы, Истина);
	Если НЕ ЗначениеЗаполнено(ГруппаСсылка) Тогда
		
		Если ВыводитьСообщения Тогда
			Сообщить("<error_details>Ошибка: Группа " + ИмяГруппы + " не найдена</error_details>");
		КонецЕсли;
		Возврат;
		
	КонецЕсли;
	
	ПоказательСсылка = Справочники.хн_нфо_ПоказателиОтчета.НайтиПоНаименованию(НаименованиеПоказателя, Истина);
	
	</validation>
	
	<indicator_creation>
	
	Если НЕ ЗначениеЗаполнено(ПоказательСсылка) Тогда
		ПоказательОбъект = Справочники.хн_нфо_ПоказателиОтчета.СоздатьЭлемент();
	Иначе
		ПоказательОбъект = ПоказательСсылка.ПолучитьОбъект();
	КонецЕсли;
	
	</indicator_creation>
	
	<data_processing>
	
	// Заполнение основных атрибутов
	ПоказательОбъект.Наименование = НаименованиеПоказателя;
	ПоказательОбъект.Родитель = ГруппаСсылка;
	ПоказательОбъект.Идентификатор = Идентификатор;
	ПоказательОбъект.ВидПоказателя = ВидПоказателя;
	ПоказательОбъект.Формула = Формула;
	ПоказательОбъект.ОписаниеПоказателя = ОписаниеПоказателя;
	ПоказательОбъект.НеВыводитьВОтчет = Ложь;
	ПоказательОбъект.ТекстКода = ТекстКода;
	ПоказательОбъект.Множитель = Множитель;
	ПоказательОбъект.ВалютаПоказателя = Undefined;
	ПоказательОбъект.ФорматДаты = "";
	ПоказательОбъект.ТипРегистратора = "";
	ПоказательОбъект.ТипПоказателяОтчета = ТипПоказателяОтчета;
	
	// Заполнение атрибутов в зависимости от вида показателя
	Если ВидПоказателя = Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле Тогда
		
		ПоказательОбъект.Регистр = "";
		ПоказательОбъект.Ресурс = "";
		ПоказательОбъект.ТипПоказателя = 1; // Строка для формульных
		
	Иначе
		
		ПоказательОбъект.ТипПоказателя = 0; // Число
		ПоказательОбъект.Регистр = "Хозрасчетный";
		
		Если ТипПоказателяОтчета = Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало Тогда
			ПоказательОбъект.Ресурс = "СуммаНачальныйОстаток";
		ИначеЕсли ТипПоказателяОтчета = Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаКонец Тогда
			ПоказательОбъект.Ресурс = "СуммаКонечныйОстаток";
		ИначеЕсли ТипПоказателяОтчета = Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод Тогда
			ПоказательОбъект.Ресурс = "СуммаОборот";
		КонецЕсли;
		
	КонецЕсли;
	
	</data_processing>
	
	<tabular_section_processing>
	
	// Заполнение табличной части для формульных показателей
	Если ВидПоказателя = Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле
		И НЕ ПустаяСтрока(СписокИспользуемыхПоказателей) Тогда
		
		ПоказательОбъект.ИспользуемыеПоказатели.Очистить();
		
		МассивИмен = СтрРазделить(СписокИспользуемыхПоказателей, ",", Ложь);
		Для каждого ИмяИспользуемого Из МассивИмен Цикл
			
			ИмяИспользуемого = СокрЛП(ИмяИспользуемого);
			Если НЕ ПустаяСтрока(ИмяИспользуемого) Тогда
				
				НоваяСтрока = ПоказательОбъект.ИспользуемыеПоказатели.Добавить();
				НоваяСтрока.Показатель = ИмяИспользуемого;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	</tabular_section_processing>
	
	<database_write>
	
	ПоказательОбъект.Записать();
	
	</database_write>
	
	<result_formation>
	
	Если ВыводитьСообщения Тогда
		Сообщить("<data_summary>Создан показатель: " + НаименованиеПоказателя + "</data_summary>");
	КонецЕсли;
	
	</result_formation>

КонецПроцедуры

//////////  СТРОКА 1 - НЕМАТЕРИАЛЬНЫЕ АКТИВЫ, ВСЕГО

// <function_purpose>Назначение: Создание показателей строки 1 отчета (Нематериальные активы, всего)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказателиСтроки1(Знач ВыводитьСообщения = Истина)

	<data_processing>
	
	СоздатьПоказатель_стр1кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол6(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол7(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол8(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол9(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол10(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол11(ВыводитьСообщения);
	СоздатьПоказатель_стр1кол12(ВыводитьСообщения);
	
	</data_processing>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателя строка 1 колонка 3 (остаток на начало)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказатель_стр1кол3(Знач ВыводитьСообщения = Ложь)

	<initialization>
	
	Идентификатор = "ГР_19_01_2025_стр1кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 22";
	
	</initialization>
	
	<indicator_creation>

	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол3,ГР_19_01_2025_стр22кол3";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	</indicator_creation>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателя строка 1 колонка 4 (поступление)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказатель_стр1кол4(Знач ВыводитьСообщения = Ложь)

	<initialization>
	
	Идентификатор = "ГР_19_01_2025_стр1кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 22";
	
	</initialization>
	
	<indicator_creation>

	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол4,ГР_19_01_2025_стр22кол4";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	</indicator_creation>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателя строка 1 колонка 5 (выбытие)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказатель_стр1кол5(Знач ВыводитьСообщения = Ложь)

	<initialization>
	
	Идентификатор = "ГР_19_01_2025_стр1кол5";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 22";
	
	</initialization>
	
	<indicator_creation>

	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол5,ГР_19_01_2025_стр22кол5";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	</indicator_creation>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателя строка 1 колонка 6 (разрешения на переоценку)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказатель_стр1кол6(Знач ВыводитьСообщения = Ложь)

	<initialization>
	
	Идентификатор = "ГР_19_01_2025_стр1кол6";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 22";
	
	</initialization>
	
	<indicator_creation>

	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол6,ГР_19_01_2025_стр22кол6";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	</indicator_creation>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателя строка 1 колонка 7 (списания переоценки)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказатель_стр1кол7(Знач ВыводитьСообщения = Ложь)

	<initialization>
	
	Идентификатор = "ГР_19_01_2025_стр1кол7";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 22";
	
	</initialization>
	
	<indicator_creation>

	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол7,ГР_19_01_2025_стр22кол7";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	</indicator_creation>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателя строка 1 колонка 8 (обесценение)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказатель_стр1кол8(Знач ВыводитьСообщения = Ложь)

	<initialization>
	
	Идентификатор = "ГР_19_01_2025_стр1кол8";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 22";
	
	</initialization>
	
	<indicator_creation>

	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол8,ГР_19_01_2025_стр22кол8";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	</indicator_creation>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателя строка 1 колонка 9 (восстановление обесценения)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказатель_стр1кол9(Знач ВыводитьСообщения = Ложь)

	<initialization>
	
	Идентификатор = "ГР_19_01_2025_стр1кол9";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 22";
	
	</initialization>
	
	<indicator_creation>

	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол9,ГР_19_01_2025_стр22кол9";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	</indicator_creation>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателя строка 1 колонка 10 (прочие изменения)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказатель_стр1кол10(Знач ВыводитьСообщения = Ложь)

	<initialization>
	
	Идентификатор = "ГР_19_01_2025_стр1кол10";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 22";
	
	</initialization>
	
	<indicator_creation>

	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол10,ГР_19_01_2025_стр22кол10";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	</indicator_creation>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателя строка 1 колонка 11 (остаток на конец)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказатель_стр1кол11(Знач ВыводитьСообщения = Ложь)

	<initialization>
	
	Идентификатор = "ГР_19_01_2025_стр1кол11";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 2 + строка 22";
	
	</initialization>
	
	<indicator_creation>

	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр2кол11,ГР_19_01_2025_стр22кол11";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаКонец,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	</indicator_creation>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателя строка 1 колонка 12 (итого)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказатель_стр1кол12(Знач ВыводитьСообщения = Ложь)

	<initialization>
	
	Идентификатор = "ГР_19_01_2025_стр1кол12";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= колонка 3 + колонка 4 + колонка 5 + колонка 6 + колонка 7 + колонка 8 + колонка 9 + колонка 10 + колонка 11";
	
	</initialization>
	
	<indicator_creation>

	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр1кол3,ГР_19_01_2025_стр1кол4,ГР_19_01_2025_стр1кол5,ГР_19_01_2025_стр1кол6,ГР_19_01_2025_стр1кол7,ГР_19_01_2025_стр1кол8,ГР_19_01_2025_стр1кол9,ГР_19_01_2025_стр1кол10,ГР_19_01_2025_стр1кол11";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	</indicator_creation>

КонецПроцедуры

//////////  СТРОКА 2 - ОБЪЕКТЫ ИНТЕЛЛЕКТУАЛЬНОЙ СОБСТВЕННОСТИ

// <function_purpose>Назначение: Создание показателей строки 2 отчета (Объекты интеллектуальной собственности)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказателиСтроки2(Знач ВыводитьСообщения = Истина)

	<data_processing>
	
	СоздатьПоказатель_стр2кол3(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол4(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол5(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол6(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол7(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол8(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол9(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол10(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол11(ВыводитьСообщения);
	СоздатьПоказатель_стр2кол12(ВыводитьСообщения);
	
	</data_processing>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателя строка 2 колонка 3 (остаток на начало)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказатель_стр2кол3(Знач ВыводитьСообщения = Ложь)

	<initialization>
	
	Идентификатор = "ГР_19_01_2025_стр2кол3";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 5 + строка 9 + строка 10 + строка 15 + строка 17 + строка 19 + строка 21";
	
	</initialization>
	
	<indicator_creation>

	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр5кол3,ГР_19_01_2025_стр9кол3,ГР_19_01_2025_стр10кол3,ГР_19_01_2025_стр15кол3,ГР_19_01_2025_стр17кол3,ГР_19_01_2025_стр19кол3,ГР_19_01_2025_стр21кол3";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОстатокНаНачало,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	</indicator_creation>

КонецПроцедуры

// <function_purpose>Назначение: Создание показателя строка 2 колонка 4 (поступление)</function_purpose>
// <parameters>Параметры: ВыводитьСообщения - Булево - Флаг вывода диагностических сообщений</parameters>
Процедура СоздатьПоказатель_стр2кол4(Знач ВыводитьСообщения = Ложь)

	<initialization>
	
	Идентификатор = "ГР_19_01_2025_стр2кол4";
	НаименованиеПоказателя = Идентификатор;
	ОписаниеПоказателя = "= строка 5 + строка 9 + строка 10 + строка 15 + строка 17 + строка 19 + строка 21";
	
	</initialization>
	
	<indicator_creation>

	СписокИспользуемыхПоказателей = "ГР_19_01_2025_стр5кол4,ГР_19_01_2025_стр9кол4,ГР_19_01_2025_стр10кол4,ГР_19_01_2025_стр15кол4,ГР_19_01_2025_стр17кол4,ГР_19_01_2025_стр19кол4,ГР_19_01_2025_стр21кол4";
 	ТекстКода = "";
	Формула = "";
	
	СоздатьИлиОбновитьПоказатель(
		Идентификатор,
		НаименованиеПоказателя,
		Перечисления.хн_нфо_ВидыПоказателейОтчета.РасчетПоФормуле,
		Перечисления.хн_нфо_ТипПоказателяОтчета.ОборотЗаПериод,
		ОписаниеПоказателя,
		СписокИспользуемыхПоказателей,
		,
		ВыводитьСообщения,
		ТекстКода,
		Формула
	);
	
	</indicator_creation>

КонецПроцедуры

// [Создание остальных процедур по аналогии будет продолжено...]
// Примечание: Полный код содержит все 55 строк с соответствующими алгоритмами